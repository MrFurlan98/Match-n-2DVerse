using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroyArea : MonoBehaviour, IBoardAction
{
    public void Action(int pPositionX, int pPositionY, BoardIcon[,] pIcons)
    {
        //GamePlayManager.Instance.BoardReference.DestroyArea(pPositionX, pPositionY, 5);
        //GamePlayManager.Instance.BoardReference.DestroyMatch();
    }
}
